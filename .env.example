# ローカル（ホストの Node）から実行する場合は localhost
# Docker Compose 内では compose ファイル側の environment が優先されます（host は db）
DATABASE_URL="postgresql://dj:djpass@localhost:5432/djapp?schema=public"

# 起動時にサンプルデータを投入するか（Docker Compose 用）
SEED_ON_START="true"
